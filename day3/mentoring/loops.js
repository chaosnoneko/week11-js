// collection --> e.g. array

const scoresBayern = [
    3,
    5, 
    6,
    7
];

// 2 styles of looping

for (let i = 0; i < scoresBayern.length; i++) {
    console.log(`Bayern scored ${scoresBayern[i]} goals`);
}